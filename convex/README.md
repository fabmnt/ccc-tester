# Welcome to your Convex functions directory!

Write your Convex functions here.
See https://docs.convex.dev/functions for more.

This project stores the results of the CCC Tester E2E runs in the `testResults`
table. The CLI submits them with the `--save-results` flag; the schema lives in
`convex/schema.ts` and the write mutation in `convex/testResults.ts`.
